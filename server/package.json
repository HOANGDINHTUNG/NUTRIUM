{
  "name": "nutrium-server",
  "version": "1.0.0",
  "description": "Backend API for Nutrium using json-server",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node node_modules/json-server/bin/index.js --watch db.json --host 0.0.0.0 --port $PORT"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.4"
  },
  "engines": { "node": ">=18" }
}
