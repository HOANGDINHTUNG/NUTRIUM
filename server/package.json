{
  "name": "nutrium-server",
  "version": "1.0.0",
  "description": "Backend API for Nutrium using json-server",
  "private": true,
  "scripts": {
    "dev": "json-server --watch db.json --port 3000",
    "start": "json-server --watch db.json --host 0.0.0.0 --port $PORT"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  },
  "engines": { "node": ">=18" }
}
